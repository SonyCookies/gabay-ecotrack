import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['192.168.254.124', 'subornative-effectually-vanna.ngrok-free.dev'],
};

export default nextConfig;
